

## KANBAN 添加Blockers和 Risks功能

1. **阻碍事项（Blockers）**：

   - **录入限制**：支持独立新建，必须且仅能关联 **“进行中（In Progress）”** 的任务。
   - **生命周期**：需由用户手动关闭。若处于未关闭状态，系统将在每日跨天时**自动延长阻碍持续天数**，并**同步自动拉长甘特图对应任务的预计工期**。

   **风险事项（Risks）**：

   - **多端录入**：支持在“甘特图任务详情”和“全局风险管理列表”双入口填写，必须关联“进行中”的任务。
   - **风险分级与排序**：引入风险等级字段（高/中/低），列表默认按 **“风险等级降序（高 $\rightarrow$ 低）”** 排列。
   - **手动闭环**：每个风险项具备独立状态控制，需用户逐项手动确认关闭。

**卡片聚合替换**：在 Kanban 卡片及甘特图视图中，将原有单一且不明确的“卡住（Stuck）”状态标，**拆分为“受阻”与“存在风险”两种独立标识**，支持直观展示当前影响状态。



加一个代办视图，代办



时间线需要单独加一个导出的功能，导出HTML文件直观看到项目的时间线