# Gantt — 设计决策记录

> 单人使用的本地甘特图桌面应用。定稿日期：2026-08-03

---

## 0. 定位

| 项 | 决定 |
|---|---|
| 使用场景 | **单人**，管理多个项目 |
| 数据 | **本地 SQLite**，不上云、不联网 |
| 交付形态 | **Tauri 2 桌面应用** |
| 产品形态 | **多视图**：甘特 / 看板 / 时间线 / 复盘（见 §0.5）。甘特只是其中一个视图 |
| 平台 | macOS 主力；Windows 保持代码级兼容（不作为第一阶段测试目标） |
| 「负责人」语义 | **纯标签字段**，不是账号，不产生权限。用于染色、筛选、负载视图 |

### 设计原则

1. **Motion-first** — 状态变化用 spring 物理动画，元素连续变形而非硬切
2. **Multi-Sensory** — 声音承载语义，不做装饰音（桌面端无触觉，视觉+听觉两条腿）
3. **Minimalist** — 表现力集中在可视区与转场；密集输入区主动克制

> ⚠️ 核心张力：甘特图是信息密集型专业工具，Expressive 风格是为低密度消费流程设计的。
> **解法：明确分区** — 左侧网格冷静克制，右侧甘特区 / 项目列表 / 转场放开表现。

---

## 0.5 多视图架构

甘特图**不是这个产品，只是它的一个视图**。四个视图共用同一批任务、同一份数据，
区别只在问的问题不同：

| 视图 | 回答什么 | 可编辑 |
|---|---|---|
| **甘特** | 这些活在时间上排在哪 | 是 —— 拖拽改期、改工期、拖进度、⌥ 标受阻、右键记一笔 |
| **看板** | 此刻谁没开始、谁被挡住了 | 是 —— 拖卡片写回底层字段；新建/关闭阻碍与风险 |
| **时间线** | 每一天到底发生了什么 | 是 —— 写/改/删当日记录 |
| **复盘** | 计划与实际差在哪、为什么 | 否，只读 |

顶层只有视图切换（⌘1–⌘4）。定义在 `src/core/views.ts`。

### 0.5.1 「计划 / 实施」降级为甘特内部的开关

它原先是顶层的两张表，现在是**甘特视图内部**的一个开关：切的是读哪一组日期、
拖拽写哪一组日期。其余三个视图各有固定的数据归属，给它们一个切了没反应的开关
比没有更糟。

随之而来的是权限模型的改变：**从"按视图"改成"按字段"**（`core/viewMode.ts`）。

| 字段 | 归属 |
|---|---|
| 计划日期 | 计划侧 |
| 实施日期、进度、受阻/风险/评论 | 实施侧 |
| **结构（增删/改名/缩进）、紧急度、负责人** | **不属于任何一侧，任何时候都能改** |

最后一行是这次改掉的。原先它们归计划侧，于是切到实施表连改个错别字都做不到；
而顶层多出看板和时间线之后，"必须先切回计划表才能新建任务"会变成每天都要撞一次的墙 ——
那两个视图里根本没有这个开关可切。

判据是：**这个字段写进哪一组数据**。写进计划日期的归计划侧，写进实施记录的归实施侧，
两组都不写的（名字、层级、负责人）不归任何一侧。

### 0.5.2 看板的列是算出来的，不是存下来的

任务上**没有 status 字段，也不会有**。四列全部由现有字段推导（`core/board.ts`）：

| 列 | 判据 |
|---|---|
| 未开始 | 没有实施日期，且进度为 0 |
| 进行中 | 有实施开始日或进度 > 0，未完成 |
| 受阻 | 有一条还没关掉的阻碍（或今天落在某段受阻区间内）|
| 已完成 | 进度 = 100% |

顺序有讲究：**完成优先于受阻**。人做完活不会记得回去关掉阻碍，按"受阻优先"
判的话看板会长期挂着一堆假警报，警报一多就没人看了。

拖动卡片不是"改状态"，而是**写回底层字段**：拖进"进行中"填实施开始日；
拖进"已完成"置进度 100% 并记下完成日；拖进"受阻"开一条阻碍，**并当场问原因** ——
默认记成「其他」的话，复盘的归因图上会有一整块无法解释的面积。

加 status 字段的代价不是多一列数据，是**多一个真相**：卡片在"已完成"而进度停在 60%，
屏幕上会同时出现两个互相矛盾的说法（同 §1.2 父任务进度只读防的是同一件事）。
更麻烦的是父任务：进度有加权汇总规则，status 没有 —— 三个子任务分别待办/进行中/完成，
父任务显示哪个？没有自然答案。代价是列固定四个，不能自定义「待评审」「待验收」。

### 0.5.2b 阻碍与风险：两件事，两个标识

原来看板上只有一个「卡住」，把两件性质不同的事糊成了一个词。现在拆开：

| | 阻碍（Blocker） | 风险（Risk） |
|---|---|---|
| 说的是 | 已经在挡路 | 可能会挡路 |
| 存在哪 | 任务行上的 `blocked` JSON（走命令栈，可 ⌘Z）| `risks` 表（即写即存，不进撤销栈）|
| 录入口 | 看板「＋ 新建阻碍」、拖进受阻列、⌥ 拖甘特条 | 任务详情、全局风险清单 |
| 准入 | 只能挂在**进行中**的活上（`board.isInProgress`）| 同左 |
| 关闭 | 手动，逐条 | 手动，逐条，**必须写处置说明**（见下）|
| 排序 | 未关闭在前 | 未关闭在前 → 等级高在前 → 记得早在前 |
| 卡片标识 | 红色，写出原因和已卡天数，可就地关闭 | 按等级取色的角标，写出条数和最高一档 |

**未关闭的阻碍会改排期。** 每日跨天时它的 `to` 推到今天，任务的计划结束日
顺延同样的天数（`blocked.extendOpenBlocks`）。这是产品要的行为，代价要说清楚：
一个标注动作从此产生了排期后果，而顺延几天并没有唯一正确答案（设备坏 3 天，
可能只损失 1 个工作日，也可能周末加班补回来了）。所以定了四条边界：

1. **只有明确标成未关闭的那种才推日期**；⌥ 拖出来的历史标注一天也不动
2. **顺延取各段的最大值，不是求和** —— 两条阻碍卡的是同一批日子，
   加起来会让一天算两次，工期凭空翻倍
3. **每推一天记在 `pushed` 里**，详情面板显示「已自动顺延 N 天」，关掉后仍留着 ——
   用户至少看得见系统替他改了什么
4. **不进撤销栈**（`CommandStack.applySystem`）。它不是谁点出来的操作，
   ⌘Z 撤掉它没有意义 —— 下一次跨天它还会回来

跨天靠**每分钟比一次 `today()`**，不是零点定时器：合盖睡眠的笔记本上，
到明天零点的 `setTimeout` 要么迟到几小时，要么被系统合并掉。窗口重新获得焦点时
也查一次 —— 跨天那一刻用户多半没看着屏幕。

**阻碍详情面板**（双击详情抽屉里的那一行打开）里填的是**起止日期**，不是天数：
天数是两个日期的差，让人去加减一个派生量，等于逼他心算。

区间两头都夹在任务之内 —— 往前不早于开工日（活还没开始就先卡住，说不通），
往后不晚于计划结束日（受阻是画在条子上的标注，飘在条子外面的一段没有视觉锚点）。
要表达「已经拖过了原计划」，该做的是先把任务拖长，再来标这一段。
`<input type="date">` 的 min/max 只挡得住点选，手打的日期照样进得来，
所以夹取必须在代码里再做一遍。

**持续中的那条不受这个上限管**：它的终点是今天，而今天是事实，不是用户填的数；
反过来是任务的计划结束日跟着它顺延（`extendOpenBlocks`）。同理，
持续中时终止日期框是禁用的 —— 让人填一个明天就会过期的日期没有意义。

面板里有两个容易被当成同一件事的控件，所以分开写：**「持续中」开关**管的是
「明天还要不要自动 +1」，**「标记为已解决」**管的是「这件事结束了」（区间收到昨天，
卡片当场离开受阻列）。合成一个开关会出事：用户关掉开关本意是「不卡了」，
结果区间还盖着今天，卡片赖在受阻列里不走，他会以为按钮坏了。

**关闭风险不是打个勾**（`migrations/008_risk_resolution.sql`）。点那个圆圈只展开
一个输入框，写清「怎么解决的」才能真的关掉；库层也拦一道空说明，因为 UI 那道
将来会被绕过，而一旦允许空说明，它就会变成默认路径。落库的是两列：
`resolved_at`（关闭时刻）和 `resolution`（处置说明）。

理由：一个 `resolved` 布尔把「这条不用管了」和「我们做了什么让它不用管」
压成了同一个比特，而后者才是复盘时唯一值得抄的东西 —— 等料风险关掉了，
到底是换了供应商、改工序绕过去，还是接受延期？三种处置的经验价值完全不同。
关闭时刻也不能靠 `updated_at` 猜：改一次措辞就会刷新它。

重新打开会把这两列一起清空。留着的话，界面上会出现一条「未关闭」却挂着
「已于 3 月 2 日解决」的风险 —— 两个互相矛盾的说法，用户从此不再相信任何一个。
两列都允许 NULL：这一列是后加的，之前关掉的风险确实没有说明，
前端照实显示「（早期记录，未留处置说明）」而不是编一句。

### 0.5.3 时间线：`day` 与 `created_at` 必须分开

时间线按**日期**组织（甘特按任务组织）。数据来自四处：

- `daily_notes` —— 手写的当日记录，可挂任务也可不挂
- 实施日期 —— 开工、完工两个事件
- 受阻时段 —— 只在**起始日**产生一条，持续天数写进事件里
- 风险 —— 按记下的那天落位

新表 `daily_notes(project_id, task_id 可空, day, content, created_at, updated_at)`
（`migrations/006_daily_notes.sql`）。两个要点：

1. **`day` 和 `created_at` 是两个不同的时间。** `day` 是"说的是哪一天"（时间线按它排），
   `created_at` 是"什么时候写的"。周五补记周三的事，两者相差两天 —— 混成一个字段，
   时间线从第一天起就是错的。前者晚于后者时界面标出「事后补记」：当时的判断和事后的追认，
   分量不一样。
2. **`task_id` 可空。** 有值 = "这件活那天怎么了"，为空 = "今天整体怎么样"。
   「今天下雨全场停工」不属于任何一条任务，硬塞给某条活会让它在复盘里被算到那条活头上。

受阻不逐天铺开：一段等料 5 天会变成 5 条一模一样的记录，把真正发生了事的那几天淹掉。

写入口有两个，落到同一张表：时间线里的「记一笔」，和**甘特条上右键某一天**。
右键是甘特上唯一还空着的手势（中间移动、两端改工期、进度手柄、⌥ 标受阻都已占用），
而且语义天然 —— 指着某一天说"这天怎么了"。

### 0.5.4 复盘：产品的高光时刻

只读。算术全在 `core/review.ts`，屏幕上的数字和导出的 Excel 必须来自同一份计算 ——
屏幕写偏差 6 天、发出去的文件写 5 天，这份东西就一文不值了。

逐条偏差的排序不是按时间也不是按名字，而是**最该被解释的排最前**：未完成的优先于
已完成的，然后按收尾偏差降序。汇报时从上往下讲，讲到没时间为止。

受阻归因按原因分别合并区间，所以**各原因天数之和可能大于日历天数** —— 同一天既在
等料又在返工是真实存在的，压成一天反而抹掉了信息。这里回答的是"等料一共拖了多少天"，
不是"日历上有多少天是废的"。

---

## 1. 数据模型语义

### 1.1 任务层级

- **N 层递归**（不写死两层），自底向上冒泡
- 建立层级的方式：大纲缩进 `Cmd+]` / `Cmd+[`

### 1.2 进度汇总

- 算法：**工期加权** `Σ(子进度 × 子工期) / Σ子工期`
- `tasks.weight` 字段保留（默认 `NULL` = 用工期），供后续手动覆盖
- **父任务进度只读**，灰显，悬停提示「由 N 个子任务汇总」
  - 理由：可手动则数据自相矛盾（父 80% / 子全 0%），用户从此不信任任何数字

### 1.3 父任务日期

- **自动汇总，只读**：`最早子任务开始 ~ 最晚子任务结束`
- **拖动父任务条 = 整组平移全部子任务**
- 父任务不能拥有独立于子任务的日期（v1 不做）

### 1.4 叶子任务进度录入（三个入口）

1. 表单里输数字
2. **直接拖甘特条上的进度手柄**（吸附到 25/50/75/100 档位）
3. 快捷键标记完成

三个入口都**只在实施视图开放** —— 进度记的是「实际干了多少」，归实施侧
（`viewMode.canEdit`）。甘特图上的手柄因此只画在实施条上，且实施日期还没填过的
虚线条（计划的影子）上不出现：那上面本来就没有进度填充可拖。

### 1.5 日历与工期

- **存储和显示均为自然日**，`工期 = 结束 − 开始 + 1`
- 但引擎日历感知：
  - 甘特图**周末画灰色底纹**
  - 自动重排落点若为周末，**吸附到下一个工作日**
  - 任务详情同时显示「自然日 7 天 / 工作日 5 天」
- 日历配置存项目级：`work_days: [1,2,3,4,5]` + `holidays: []`（手动添加，不接第三方 API）

### 1.6 日期三值联动（只有两个自由度）

| 用户操作 | 保持不变 | 变化 |
|---|---|---|
| 拖动条子整体平移 | 工期 | 开始 + 结束一起移 |
| 拖动条子左边缘 | 结束日期 | 开始日期 + 工期 |
| 拖动条子右边缘 | 开始日期 | 结束日期 + 工期 |
| 表单里改开始日期 | **工期** | 结束日期跟着推 |
| 表单里改工期 | 开始日期 | 结束日期 |

### 1.7 紧急程度

- 四档：`P0 紧急 / P1 高 / P2 中 / P3 低`
- 视觉：甘特条**左端 3px 竖色标** + 网格行首小圆点（不整条染色，避免与负责人色、基线色打架）
- P0 额外带极轻呼吸动效（1.2s 周期，opacity 0.7↔1.0）——只有 P0 用，稀缺才有信号价值

---

## 2. 依赖与调度（v2）

### 2.1 依赖类型

- **实现只支持 FS（完成→开始）+ lag**，覆盖约 90% 真实场景
- **schema 按完整 FS/SS/FF/SF 设计**，`type` 字段现阶段只允许存 `'FS'`——将来扩展无需迁移

### 2.2 约束强度：软约束 + 冲突可视化

依赖约束的是**计划层（日期）**，不是**执行层（进度）**。

> A 未完成时 B **可以**开始（fast-tracking / 并行赶工是常态）。
> 硬约束会逼用户删依赖来绕开，一旦如此，关键路径 / 基线 / 连锁重排全部失效。

冲突呈现：

- 连线变红 + 警示标记 + **低频「咔」音**（与吸附音明确区分）
- 顶部「计划健康度」：`3 处依赖冲突 · 2 个任务逾期`，点开是列表，点条目跳转定位
- 分级：**抢跑**（日期重叠但下游未开工）标黄；**已违反**（上游未完成而下游进度 > 0）标红

### 2.3 连锁重排：预览 + 确认

- 拖动过程中，下游任务实时显示**半透明幽灵条**在新位置
- 松手才真正落定，浮层提示「已重排 7 个任务 · 撤销」
- 需配 `tasks.pinned`（锁定）字段：锁定任务不参与自动重排，上游推过来直接判冲突

---

## 3. 基线（v2）

### 3.1 存什么

**逐任务快照，只存日期，不存进度**（存进度会滑向 EVM 挣值管理，量级过大）。

```sql
CREATE TABLE baselines (
  id          INTEGER PRIMARY KEY,
  project_id  INTEGER NOT NULL,
  name        TEXT    NOT NULL,   -- "初始计划" / "3月复盘版"
  created_at  TEXT    NOT NULL
);

CREATE TABLE baseline_tasks (
  baseline_id INTEGER NOT NULL,
  task_id     INTEGER NOT NULL,
  start_date  TEXT    NOT NULL,
  end_date    TEXT    NOT NULL,
  duration    INTEGER NOT NULL,
  PRIMARY KEY (baseline_id, task_id)
);
```

### 3.2 多基线

支持多条记录，**UI 一次只对比一条**（顶部下拉：初始计划 / 3月复盘版 / 无）。

### 3.3 显示：默认关闭 + 一键切进「对比模式」

- 基线是**诊断工具，不是日常视图**，常挂只增噪音
- 开启后：主条下方 1/3 高灰条，**颜色编码偏差方向**（延后=橙 / 提前=绿），两端画偏移连接线 + 标注 `+6d`
- 切换转场（主条上移、灰条下方滑出、偏差数字淡入）是 Motion-first 的低成本高观感落点

### 3.4 项目级汇总（基线的真正价值）

顶部一行：`相比「初始计划」整体延后 6 天 · 12 个任务偏移 · 最大偏差：接口联调 +14d`

全部由逐任务数据**派生**，不存储：

| 指标 | 算法 |
|---|---|
| 整体延后天数 | `MAX(当前.end) − MAX(基线.end)` |
| 偏移任务数 | `COUNT(当前.start ≠ 基线.start OR 当前.end ≠ 基线.end)` |
| 最大偏差任务 | `MAX(当前.end − 基线.end)` |

### 3.5 边界情况

- 基线之后新增的任务：正常显示 + 小「新增」角标
- 基线里有但已删除的任务：默认不显示；对比模式下可勾选「显示已删除」，虚线灰条呈现

---

## 4. 存储与项目管理

### 4.1 单库多项目

- 一个 `gantt.db`，`projects` 表 + 所有任务带 `project_id`
- 理由：备份 = 一个文件；**跨项目视图成为可能**（"这周我全局要交付什么" / "张三在 3 个项目里的总负载"）
- 单项目分享靠 Excel / PDF 导出覆盖

### 4.2 备份（本地存储的最大风险是静默丢失）

- 每次启动滚动备份至 `backups/gantt-YYYY-MM-DD.db`，保留最近 10 份
- 设置里提供「在访达中显示数据文件」

### 4.3 项目列表界面

```
┌─────────────────────────────────────────────┐
│  我的项目                        [+ 新建项目] │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ ◗ 68%        │  │ ◗ 12%        │         │
│  │ 官网改版      │  │ App v2.0     │         │
│  │ ▁▃▅▇▅▃▁      │  │ ▁▁▃▅▇▇▅      │ ← 迷你甘特缩略图
│  │ 24 任务 · ⚠2 │  │ 51 任务       │         │
│  │ 3.15 – 6.30  │  │ 5.01 – 12.20 │         │
│  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────┘
```

- 卡片上**迷你甘特缩略图**：一眼看出时间形状与密度
- 每个项目建时选一个强调色，进度环用它，并贯穿到甘特图形成识别
- 卡片直接显示**逾期数**，不用进去才发现着火
- 点击卡片 → **卡片放大变形展开成甘特工作区**（`layoutId` 共享元素，~400ms spring）—— 全应用第一印象，值得花力气

---

## 5. 左侧输入区：网格为主 + 详情面板

**批量录入速度是甘特工具的生死线。** 表单式录 80 个任务要点 400 次鼠标，用户会退回 Excel。

- **网格列（高频）**：任务名 · 开始 · 结束 · 工期 · 进度 · 负责人 · 紧急度
- **详情面板（低频）**：点击行 → 右下滑出，放依赖、权重覆盖、备注、锁定

### 键盘流是一等公民

| 键 | 行为 |
|---|---|
| `Enter`（末行） | 新建下一行任务 |
| `Tab` / `Shift+Tab` | 跳字段 |
| `Cmd+]` / `Cmd+[` | 缩进 / 取消缩进（= 建立父子层级，比拖拽快得多） |

- **支持从 Excel 直接粘贴多行单元格**——比走文件对话框顺手十倍，是最好的导入入口
- **网格行高必须与右侧甘特行高严格对齐**，两侧同步滚动（折叠/分组时最易错位）

---

## 6. 渲染架构

### 6.1 混合方案：Canvas 画图 + DOM 画交互

DOM 全量渲染在 WKWebView 上必掉帧（500 任务 ≈ 数千节点）；纯 Canvas 则文字编辑成本过高。

- **Canvas**：网格、周末底纹、甘特条、进度层、依赖线、基线条
- **DOM**：悬停卡片、行内编辑框、右键菜单、拖拽把手命中区
- 两层严格共用同一套坐标变换

### 6.2 Canvas 三层，各自独立重绘

| 层 | 内容 | 何时重绘 |
|---|---|---|
| 静态层 | 网格线、周末底纹、月份分隔、今天线 | 仅缩放 / 平移时 |
| 内容层 | 甘特条、进度、依赖线、基线条 | 数据变化或缩放时 |
| 交互层 | 幽灵条、吸附高亮、连线预览、框选 | 每帧（内容极少） |

> 拖拽时**只有交互层每帧重绘**——这是"不掉帧"的关键，而非优化单次绘制。

### 6.3 缩放模型：单一状态变量 `pxPerDay`

「按日/周/月/年切换」和「`Cmd+滚轮` 连续缩放」**不是两个功能，是同一个功能的两种入口**：

- 滚轮 → 指数曲线连续改变：`pxPerDay *= 1.0015 ** Δ`
- 点「周」按钮 → spring 动画把 `pxPerDay` 平滑推到 `12`

刻度层级由 `pxPerDay` 自动派生：

| `pxPerDay` | 主刻度 | 次刻度 |
|---|---|---|
| ≥ 24 | 日 | 周 |
| 8 – 24 | 周 | 月 |
| 2 – 8 | 月 | 季 |
| 0.5 – 2 | 季 | 年 |

阈值附近 ±15% 区间内**两套刻度按 opacity 交叉淡入淡出**，而非硬切 —— 即"像地图一样平滑演化"。

### 6.4 三个决定体感的细节

1. **缩放锚点必须是鼠标位置**（不是视口中心、不是左边缘）。做错则整个手感报废
2. **缩放要有阻尼**：滚轮停下后 `pxPerDay` 继续 spring 衰减一小段再停
3. **Tauri 中必须彻底禁用 WebView 页面缩放**，否则 `Cmd+滚轮` 被系统抢走

### 6.5 规模假设

按单项目上限 **~500 条任务**设计：行虚拟化做；依赖线空间索引不做（暴力遍历足够）。

---

## 7. 撤销 / 重做

- **所有数据变更走命令模式**：`Command { do(), undo() }`
- 一次连锁重排打包成**一条复合命令** —— 撤销一次整体回滚 7 个任务，而不是撤 7 次
- 必须从第一行业务代码就这么写，事后补极痛

---

## 8. 音效体系（v2）

> 做不好的多感官设计 = 噪音。用户第一天就去设置里关掉，前功尽弃。

### 8.1 音源：程序化合成（Web Audio `OscillatorNode` + 包络）

零资源文件、延迟极低、跨平台一致，且**可参数化**——这是采样做不到的：

- 拖动进度手柄时**音高随百分比线性上升**（400Hz → 900Hz），闭眼可辨
- 吸附到不同对象用不同音高：网格线 = 高音短促；依赖边界 = 中音带共鸣

### 8.2 语义音效表

| 事件 | 声音 | 触发频率 |
|---|---|---|
| 吸附对齐 | 1200Hz · 25ms · 极轻 | 高 |
| 拖到依赖冲突 | 180Hz · 60ms · 低沉「咔」 | 中 |
| 进度手柄拖动 | 400→900Hz 连续变调 | 拖动中持续 |
| 任务标记完成 | 上行三音（C-E-G） | 低 |
| 连锁重排落定 | 柔和 whoosh + 极轻计数点击 | 中 |
| 撤销 | 下行二音 | 中 |
| 删除 | 低沉短音 | 低 |
| 导出成功 | 完成和弦 | 极低 |
| **缩放层级切换** | **不发声** | — |

> 最后一行是刻意的：缩放是连续高频操作，加音必吵。
> **"哪里不该有声音"与"哪里该有"同等重要。**

### 8.3 三条护栏

1. **节流**：同一音效 60ms 内不重复触发（拖拽吸附一秒能触发十几次）
2. **默认音量 25%**，全局静音快捷键 `M`，设置里可分类关闭
3. **首次用户交互后才初始化 `AudioContext`**；音频节点池化复用，不每次 `new`

---

## 9. 动效规范

全局只用三档 spring，**不用 ease 曲线**：

| 档位 | 参数 | 用途 |
|---|---|---|
| `snappy` | stiffness 400, damping 30 | 按钮、悬停、吸附反馈 |
| `smooth` | stiffness 200, damping 26 | 面板滑出、缩放档位跳转 |
| `dramatic` | stiffness 120, damping 20 | 项目卡片展开成工作区 |

**铁律**：尊重 `prefers-reduced-motion`，开启时全部降级为 120ms 淡入淡出。

动画只用 `transform` / `opacity`，绝不动 `width` / `left` / `top`。

---

## 10. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 壳 | **Tauri 2** | 10MB 包体；可彻底关闭页面缩放（`Cmd+滚轮` 归自己）；原生文件写入 |
| 前端 | **React 19 + TypeScript + Vite** | — |
| 数据库 | **rusqlite（bundled）+ 自建迁移** | 见下方「实施期变更」 |
| 状态 | **Zustand** + 自建命令栈 | Redux 过重；Undo 自己写更可控 |
| 样式 | **Tailwind v4**（CSS 变量做 token 层） | 项目主题色靠 CSS 变量注入 |
| 动效 | **Motion**（原 Framer Motion） | 仅用于 DOM 层；Canvas 内部自写 RAF |
| 日期 | **date-fns** | tree-shakable、纯函数，配合工作日历逻辑 |
| Excel | **SheetJS (xlsx)** | 导入导出同一套 |
| PDF | **Canvas 重绘 → jsPDF 嵌入** | 复用甘特渲染器，所见即所得 |

### 跨平台注意（Tauri 与 Electron 的核心取舍）

Tauri 用**系统 WebView**（macOS = WKWebView / Safari 内核；Windows = WebView2 / Chromium 内核），
所以同一份代码跑在两个不同引擎上 —— 对高频动画应用是真实风险。应对：

1. 甘特主区用 **Canvas**（跨内核性能一致得多）
2. 动画只用 `transform` / `opacity`
3. **快捷键从第一行起就用 `mod` 抽象**（`Cmd` ↔ `Ctrl`），不硬编码
4. 桌面端拿不到触觉反馈 —— 多感官实际只剩视觉 + 听觉，音效设计因此承担更多权重
5. **不能交叉编译**：双平台出包需 GitHub Actions 跑双平台构建
6. **应用内拖拽不用 HTML5 draggable，一律用指针事件**：Windows 上 Tauri 默认接管窗口的拖放
   （项目列表要接住从资源管理器拖进来的 .ganttproj / .db），代价是网页里的 HTML5 拖拽整个失效；
   macOS 上却一切正常，这类 bug 只有换到另一台机器才看得见

---

## 11. 导出 / 导入

### Excel

- 两个 sheet：`任务`（可回导，含 ID 列）+ `说明`（字段规则）
- **导入用同一模板**：有 ID 则更新，无 ID 则新建

### 时间线 · 单文件 HTML（已实现）

时间线为什么不并进那张 Excel：Excel 那份按**任务**排（一行一件活 + 一条甘特条），
是给人核排期的；时间线按**日期**排，正文是一段段人写的话，塞进单元格只会被截断，
读的人得挨个点开格子。两种问法，两个出口。

- 样式与脚本全内联，**零外部请求** —— 导出件的去处是微信、邮件附件、共享盘
- **没有 JS 也是完整的一份**：默认全部展开、按天倒序；筛选与正/倒序是脚本加的增强
- 活动热力图（一列一周、一格一天）回答时间线本身答不了的问题：哪几段时间没动静
- 聚合仍走 `core/timeline.buildTimeline`，和屏幕上是同一份结果 —— 重算一遍迟早对不上
- 用户手写的正文一律 HTML 转义：一句「压力 < 0.3MPa」不转义会让它之后的内容全部消失

### PDF

- 导出前配置：时间范围 / 是否含基线 / 是否分页 / 纸张方向
- 用同一 Canvas 渲染器按 300dpi 重绘，保证与屏幕完全一致
- 页眉：项目名 + 导出日期 + 图例

### 项目文件 `.ganttproj` · 跨电脑传输（已实现）

上面三样都是**给人看**的排版件，共同点是有损：为了排版好看，评论、逐日记录、
头像、依赖全都不进去，也回不来。`.ganttproj` 解决的是另一个问题 ——
**同一个人在两台电脑上接着干同一个项目**。

**它和 Excel 导入不是同一件事，别合并。** Excel 那条路服务的是「别人给我一张表，
我要把它变成任务」，输入是人类编辑过的、结构松散的东西，必然要容错、要猜、
要允许部分成功。项目文件服务的是「我自己的数据搬个家」，输入是本软件自己写出来的，
一个字节的偏差都意味着出了问题。两种输入的正确策略正好相反，做成一个只会两头不讨好。

#### 载体：单文件 zip，不是文件夹

文件夹在传输里是最差的载体 —— 微信、邮件、U 盘都要先压缩，而且丢一个文件就变成
静默损坏的半成品。`.sketch` / `.docx` / `.xmind` / `.key` 无一例外都是
「对外一个文件、内部目录结构」的 zip，这里照做：

```
manifest.json     格式版本、导出时间、项目名与 uuid（不解开就能预览）
project.json      项目 + 任务 + 依赖 + 风险 + 评论 + 逐日记录 + 基线
people.json       只含本项目用到的负责人
avatars/*.png     头像还原成真实图片文件
```

JSON 一律 pretty-print：缩进是全世界最好压的东西，体积代价被 deflate 吃掉了，
换来的是「改名成 .zip 解开就能读、能 diff、必要时能手改」。一个自己都打不开的
备份格式，出事时给不了任何安全感。

头像不留在 JSON 里而是还原成 PNG：base64 内嵌会让文件膨胀 33%，而且一个几十 KB
的字符串横在 JSON 中间，上面说的「能读」就白说了。

#### 整件事在 Rust 侧做

和 Excel / 时间线相反 —— 那两个的内容依赖着色器、工作日历、汇总结果这些**只存在于
渲染层**的东西，在 Rust 里重算等于把模型实现两遍。项目文件没有任何这类依赖，
数据本来就在 SQLite 里，让前端把几 MB 头像捞出来、编码、再传回去纯属浪费。

#### 项目身份：uuid 优先，名字兜底

导入时必须回答「这是本机某个项目的新版本，还是一个全新项目」。

- 只靠名字答不了：你在另一台电脑上改了名，带回来就认不出
- 只靠 uuid 也答不了：这个功能上线**之前**，两台机器上那两个「同一个项目」
  是各自建的，uuid 各自随机，第一次导入必然对不上

所以两者都要，uuid 命中优先，没命中再比名字。名字能当兜底的前提是它唯一，
于是 `projects.name` 升成硬约束（`migrations/007`）。副作用是新建/重命名要挡重名 ——
那是好事：一旦允许「厂房建设」和「厂房建设 (2)」并存，名字作为身份线索就废了。

uuid 的生成规则放在 schema 的触发器里，不放在调用方。写在调用方就意味着
「以后每条插入路径都得记得」，而这类「记得」迟早有人不记得。

#### 全有或全无

写库前把整份数据校验一遍，一条不合格就整个拒绝，并用人话报出是哪几条 ——
不靠数据库的 CHECK 兜底（那时用户看到的是 `SQLITE_CONSTRAINT`，既不知道哪条
任务有问题也不知道该怎么办）。

不做「跳过坏行」是因为可发现性：47 个任务导进来 45 个，用户不会去逐条核对，
只会在三周后发现某个任务凭空消失了 —— 而那时源文件早删了。

#### 覆盖的后悔药

覆盖前把本机版整份导成 `.ganttproj` 存进 `backups/`，列表页给一条可撤销的横幅。
**撤销走的是和正常导入完全相同的那条路** —— 把那份备份再导入一次即可。
没有第二套恢复逻辑：一份只在出事时才被走到的代码路径是不可信的。

不复用 `backup::rotate`：它是整库 `.db`，当天已有备份就跳过（所以覆盖前调它是个
空操作），而且拿它恢复一个项目要关应用、换文件、重启，还会连带回滚掉其他项目。

#### 格式版本：major.minor

major 变 = 语义改了或字段删了，旧版**必须拒绝**；minor 变 = 只加了字段，
旧版照常导入并提示忽略了什么。

这不是形式主义：跨电脑的主线场景就是「两台机器上的 Gantt 版本不一样」。
单个整数版本号会让「加了一个无关紧要的字段」把旧机器彻底锁死。

#### 往返幂等是唯一那条真正重要的测试

文件里的任务 id 一律重编号成 1..n（库里的全局自增 id 带出去毫无意义），
于是「导出 → 导入 → 再导出，两次 project.json 逐字节相同」成为一条可断言的不变量。

一条断言同时挡住三类 bug：某字段没被导出、某字段导入时丢了、排序不稳定。
写十个「assert 某字段还在」的零散断言也覆盖不到第三类。

---

## 12. 阶段划分

### v1 · 能用（当前目标）

- 项目 CRUD + 项目列表页
- 任务网格（键盘流、N 层父子、进度加权汇总）
- Canvas 甘特图 + `pxPerDay` 连续缩放
- 拖拽改期（整体 / 左缘 / 右缘 / 进度手柄）
- **Undo/Redo（命令模式）**
- 今天线 · 周末底纹 · 里程碑 · 折叠展开
- SQLite + 自动滚动备份

> 缺任何一条都不算一个可用的甘特工具。

### v2 · 好用

- FS 依赖 + 冲突可视化（抢跑黄 / 违反红）
- 连锁重排幽灵条预览 + 任务锁定
- 基线对比
- 音效体系
- 项目卡片展开转场

> 差异化真正所在，但必须站在 v1 的地基上 —— 拖拽稳定、依赖存在、Undo 可靠之后再做。

### v3 · 加分

- 关键路径高亮
- 资源负载泳道
- PDF / Excel 导入导出
- 筛选 / 搜索 / 高亮
- 跨项目视图

---

## 11. 甘特条着色

### 11.1 被否掉的方案：按行号保证相邻不同色

最初的想法是「确保相邻两行的条子颜色不同」。否掉，三个理由：

1. **颜色是稀缺资源，且已经超载。** 条子区域的颜色已经承载进度、父子、紧急度、
   里程碑、今天/节假日；v2 还要加基线偏差和依赖冲突。再塞一个「相邻区分」，
   用户看到橙条时将无法判断它是「延后了」还是「只是轮到橙色了」——
   一个通道同时表达两件事，等于两件事都表达不了。
2. **它优化的「相邻」是错的相邻。** 真正容易混淆的是**时间轴上重叠**的条子，
   而它们在屏幕上往往隔着十几行；屏幕上紧挨的两行若一个在 3 月一个在 9 月，
   本就隔着半个屏幕的横向距离，不需要颜色区分。
3. **一维列表上「相邻不同」平凡地由两色交替满足** —— 那就是斑马纹，已经有了。
   真正的诉求其实是「每条都容易被单独辨认」，那需要的色数远超 2，会变成彩虹噪音。

> **不用「位置」决定颜色，用「数据」决定颜色。**

### 11.2 采用的方案：可切换的语义着色

工具条提供四种着色依据，存全局 `settings.bar_color_by`（视图偏好，
不进撤销栈、不随项目走、不被导出带走）：

| 依据 | 分组键 | 说明 |
|---|---|---|
| **阶段**（默认） | 顶层祖先 | 色数被阶段数天然限制；相邻跨阶段条子自然不同色 |
| 负责人 | assignee | 同一个人跨阶段同色；空值走中性灰，不占调色板 |
| 紧急度 | priority | **固定映射**，不参与顺序分配 —— P0 永远是红的 |
| 单色 | — | 项目强调色。导出 PDF / 灰度打印时彩色反而是干扰 |

- 调色板 **8 色，明度刻意错开**：色觉障碍与灰度打印下仍能靠深浅区分
- 色相按分组**首次出现顺序**分配，**不用 id 哈希** ——
  哈希会导致调整一次任务顺序整屏变色，用户会以为数据坏了

### 11.3 进度与色相分走不同视觉通道

| 通道 | 表达 |
|---|---|
| 色相 | 归属（属于哪个阶段 / 谁） |
| 明度对比 | 进度：已完成 = 该色 100%，未完成 = 同色 18% |

「60% 完成的设计阶段任务」= 60% 实心紫 + 40% 淡紫。两个信息互不干扰。
条内文字颜色按填充色亮度自动反转。

### 11.4 紧急度让出视觉通道

条子本身有色之后，原来的 3px 紧急度竖标会变成「两个不相关的色相贴在一起」。

**决定：甘特条上只标 P0**，用**形状 + 颜色双编码**（左端红色小三角，抗色盲、
抗灰度打印）。P1–P3 不在条上出现 —— 真实项目里绝大多数任务是 P2，
四档铺满全屏几乎不传递信息却占着一整个通道。**稀缺才有信号价值**
（与 §1.7 的「呼吸动效只给 P0」同一条原则）。完整的紧急度信息仍在左侧网格圆点上。

着色依据切到「紧急度」时，P0 三角自动隐藏（此时整条已是紧急度色，三角纯冗余）。

### 11.5 时间重叠的辨识：不用颜色解决

甘特图的固有矛盾：**垂直顺序表达的是结构（WBS），不是时间**。
时间上重叠的任务可以隔着任意多行。重排行序解决不了（会破坏层级），
只能把时间维度显式画出来：

| 手段 | 做法 |
|---|---|
| **时间游标** | 跟随鼠标的竖虚线贯穿全图 + 顶部日期胶囊，吸到整天格中心（否则读数会随亚像素抖） |
| **同期高亮** | 悬停某条时，所有**时间上与它重叠**的条子加描边。父任务不参与——它的区间是子任务并集，几乎和一切重叠，标出来只是噪音 |

两者都只画在**交互层**，所以移动鼠标不触发条子重绘，拖拽性能不受影响。

---

## 附：实施期的决策变更

定稿之后，写代码时发现三处需要偏离原方案。记录在此，理由随代码一起可查。

### 变更 1：`tauri-plugin-sql` → rusqlite + 自建迁移

**问题**：plugin-sql 通过连接池执行 SQL，`BEGIN` 和 `COMMIT` 可能落在不同连接上，
**拿不到可靠的事务**。而撤销一次要同时改回二十几行，这个操作必须是原子的。

**决定**：直接用 rusqlite，前端拿到的是一组类型化的 Tauri 命令而不是任意 SQL 通道。
附带收益：schema 只有 `src-tauri/src/db.rs` 一个知情方，渲染层不可能写出跟不上迁移的 SQL；
迁移用 SQLite 自带的 `user_version` 记录，不额外建表。

### 变更 2：写入采用「整项目事务性替换」，不做增量 SQL

**问题**：前端的真相是内存 TaskMap + 命令栈。一次撤销可能同时改回 20 行、
恢复 3 行被删的、再撤掉 1 行新增的。把这翻译成精确的增量 SQL，等于在前端和数据库之间
再维护一套同步状态机 —— 它一旦有 bug 就是**静默的数据损坏**。

**决定**：每次落库在一个事务里整体替换该项目的 tasks 与 dependencies。
500 个任务全量替换在本地 SQLite 上是几毫秒，用一个数量级的性能余量换掉一整类同步 bug。
配套：400ms 防抖 + 窗口失焦/关闭强制冲刷 + 落库失败时保留待写标记（不假装已保存）。

### 变更 3：备份必须走 SQLite 在线备份 API，不能 `fs::copy`

**问题**：数据库跑在 WAL 模式，事务先落到 `gantt.db-wal`，要 checkpoint 才合并回主文件。
原实现用 `fs::copy` 复制主文件，实测复制出来的是**一张表都没有的空壳**。
正常关闭时看不出来（SQLite 会自动 checkpoint），但进程被强杀或崩溃时 ——
恰恰是最需要备份的时候 —— 备份是废的。

**决定**：改用 `rusqlite::backup::Backup`，从活动连接读取，天然包含 WAL 内容。
备份时机也从「打开数据库之前」改成「之后」。
有专门的测试：故意不 checkpoint，断言备份数据完整。

> 一个静默产出空备份的备份功能，比没有备份更危险 —— 它给的是虚假的安全感。

### 变更 4：日期在库里存 ISO 字符串，在内存里用天序号

两种表示各有不可替代的理由，转换收敛在 `src/db/convert.ts` 一层：

| | 表示 | 理由 |
|---|---|---|
| 数据库 | `'2026-08-03'` | 自描述。用 DB 浏览器打开、或走 Excel 导入导出时看到的必须是日期，不是 `9711`。天序号还隐含依赖代码里的纪元常量，改动即全库失效 |
| 内存 | `9711`（浮点） | 缩放/平移需要连续插值和像素换算 |

---

## 附：相对飞书甘特图的差异化清单

| # | 差异点 | 飞书现状 | 阶段 |
|---|---|---|---|
| 1 | 连续平滑缩放（像地图） | 日/周/月 三个硬切按钮 | v1 |
| 2 | 拖拽吸附 + 语义化音效 | 无 | v2 |
| 3 | 基线对比 + 项目级偏差汇总 | 无 | v2 |
| 4 | 连锁重排的幽灵条预览 + 一键撤销 | 硬推、无预览、无撤销提示 | v2 |
| 5 | 计划健康度（冲突/逾期一览） | 既不阻止也不提示 | v2 |
| 6 | 完整 FS 依赖 + lag | 弱前置任务 | v2 |
| 7 | 关键路径 | 无 | v3 |
| 8 | 资源负载热力图 | 无 | v3 |
| 9 | 全键盘操作 | 基本不可键盘操作 | v1 |
| 10 | 大数据量不掉帧（Canvas 分层） | 上量即卡 | v1 |
| 11 | 跨项目视图 | 项目即孤岛 | v3 |
